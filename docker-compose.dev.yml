version: '3.8'

# SECURITY: This is the ONLY allowed Docker configuration
# Production docker-compose.yml has been disabled for security

services:
  # Node.js Backend Server (Development ONLY)
  server-dev:
    build:
      context: ./Server
      dockerfile: Dockerfile.dev
    container_name: nodejs-server-dev
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"  # Localhost only for security
    environment:
      - NODE_ENV=development
      - PORT=3001
      - LOG_LEVEL=debug
      - ENABLE_FILE_LOGGING=true
      - RATE_LIMIT_WINDOW_MS=900000
      - RATE_LIMIT_MAX_REQUESTS=1000
      - SECURITY_MODE=development_only
    volumes:
      - ./Server/src:/app/src
      - ./Server/logs:/app/logs
      - server-node-modules:/app/node_modules
    networks:
      - app-network
    command: npm run dev

  # React Frontend Client (Development ONLY)
  client-dev:
    build:
      context: ./Client
      dockerfile: Dockerfile.dev
    container_name: react-client-dev
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"  # Localhost only for security
    environment:
      - VITE_API_URL=http://localhost:3001
      - NODE_ENV=development
    volumes:
      - ./Client/src:/app/src
      - ./Client/public:/app/public
      - ./Client/index.html:/app/index.html
      - ./Client/vite.config.js:/app/vite.config.js
      - client-node-modules:/app/node_modules
    networks:
      - app-network
    depends_on:
      - server-dev
    command: npm run dev

networks:
  app-network:
    driver: bridge
    name: project-dev-network

volumes:
  server-node-modules:
    driver: local
  client-node-modules:
    driver: local
